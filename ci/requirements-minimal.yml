name: recursive_diff
channels:
  - conda-forge
dependencies:
  - pytest
  - pytest-cov
  - packaging

  - dask=2.0
  - numpy=1.16
  - pandas=0.25
  - xarray=0.12

  # NetCDF engines
  - scipy=1.3
  - h5netcdf=0.7
  - hdf5=1.10
